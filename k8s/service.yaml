apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  namespace: prod
  labels:
    app: my-app
spec:
  type: ClusterIP
  selector:
    app: my-app
    version: blue  # initial production traffic goes to BLUE
  ports:
    - name: http
      port: 80
      targetPort: 8080
